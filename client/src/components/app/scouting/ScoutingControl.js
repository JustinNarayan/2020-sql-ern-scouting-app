import React, { useState } from "react";

const ScoutingControl = () => {
   return (
      <p style={{ color: "red" }}>
         Hello there, this is pretty long so i'm just checking what's going to
         happen
      </p>
   );
};

export default ScoutingControl;
